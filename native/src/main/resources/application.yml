server:
  port: 8081
spring:
  datasource:
    url: 'jdbc:mysql://localhost:3306/native?useSSL=false'
    username: 'root'
    password: 'root'
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: 'org.hibernate.dialect.MySQL57InnoDBDialect'
