spring:
  datasource:
    url: 'jdbc:mysql://localhost:3307/jvm?useSSL=false'
    username: 'root'
    password: 'root'
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: 'org.hibernate.dialect.MySQL57InnoDBDialect'
